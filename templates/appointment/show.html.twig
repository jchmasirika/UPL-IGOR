{% extends 'base.html.twig' %}

{% block title %}Appointment{% endblock %}

{% block body %}
    <h1>Rendez-vous</h1>

    <a class="btn btn-secondary" href="{{ path('app_appointment_edit', {'id': appointment.id}) }}">Modifier</a>
    <hr/>

    <table class="table">
        <tbody>
            <tr>
                <th>Numéro</th>
                <td>{{ appointment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ appointment.date ? appointment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ appointment.description }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('appointment/_delete_form.html.twig') }}
{% endblock %}
